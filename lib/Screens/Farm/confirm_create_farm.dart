import 'package:flutter/material.dart';

class ConfirmCreateFarm extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return Center(
      child: Text('confirm'),
    );
  }
}
